package com.example.gestionrepositorios.servicio;

/**
 * Contrato para el servicio que demuestra el uso de los repositorios.
 */
public interface IServicioProyecto {
    // En esta demo, la lógica principal se ejecuta en @PostConstruct
    // por lo que la interfaz puede permanecer vacia o tener otros métodos de negocio
}
